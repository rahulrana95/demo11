const config = {
  baseURL: 'https://jsonmock.hackerrank.com/api/',
  apiEndPoint: {
    fetchStockInfo: 'stocks',
  },
};

// https://jsonmock.hackerrank.com/api/stocks?date=

export default config;
